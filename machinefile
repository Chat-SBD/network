# cluster setup:
# `ssh node1, node2, node3, etc...`
# `sudo mount -t nfs beowulf:/usr /usr`
# `sudo mount -t nfs beowufl:/etc/alternatives /etc/alternatives`

# `mpiexec -n 5 -f machinefile <command> <command args...>`
# to test cluster connection: `mpiexec -n 5 -f machinefile python3 flops.py 5`

beowulf:1
node1:1
node2:1
node3:1
node4:1